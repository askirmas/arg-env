{
  "name": "arg-env_e2e",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "run": "node --require=arg-env script.js --env-file=1.env --env-file=2.env",
    "test": "jest",
    "pretest": "npm install"
  },
  "config": {
    "env_file": [
      "3.env",
      "./4.env"
    ]
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "arg-env": "file:../package",
    "jest": "file:../node_modules/jest"
  }
}
